{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch .//css/style.scss:./assets/css/style.css",
    "prefix": "postcss ./style.css --use autoprefixer -d ./prefixed/",
    "start": "npx http-server . -p 4000 -P \"http://localhost:4000?\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bourbon": "^7.0.0"
  }
}
